Đại diện cho một vùng nhớ (rg = region).
# Tham số
- `long rg_start`: nơi bắt đầu của vùng nhớ.
- `long rg_end`: nơi kết thúc của vùng nhớ.
- Con trỏ [[vm_rg_struct]] `rg_next` trỏ đến thanh ghi tiếp theo.